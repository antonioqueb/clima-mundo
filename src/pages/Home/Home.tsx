import React from 'react';
import WeatherDisplay from '../../components/WeatherDisplay/WeatherDisplay';

const Home: React.FC = () => {

    return (
        <div>
            <WeatherDisplay />
        </div>
    );
};

export default Home;
